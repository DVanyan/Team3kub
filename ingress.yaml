apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: "devops-intermidate"
  namespace: "devops-intermidate"
  annotations:
    kubernetes.io/ingress.class: alb
  labels:
    app: nginx
spec:
  rules:
    - host: menq.meroncov.com
      http:
        paths:
          - path: /*
            backend:
              serviceName: "devops-menq-meroncov"
              servicePort: 80
